services:
  - type: web
    name: youtube-ai-clone
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
    plan: free 